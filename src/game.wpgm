import wollok.game.*
import tanque.*
import escenarios.*
import enemigos.*
import config.*

program game {
	game.title("Wollanzerkampfwagen")
	game.height(13)
	game.width(13)
	game.cellSize(50)
	nivelUno.iniciar()
	game.start()
}
